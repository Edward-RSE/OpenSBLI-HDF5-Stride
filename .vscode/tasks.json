{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build wave",
            "type": "shell",
            "command": "./build.sh",
            "options": {
                "cwd": "${workspaceFolder}/src-wave"
            }
        }
    ]
}