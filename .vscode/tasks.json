{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build slice example",
            "type": "shell",
            "command": "./build.sh",
            "options": {
                "cwd": "${workspaceFolder}/src-3d-slice-example"
            }
        },

    ]
}