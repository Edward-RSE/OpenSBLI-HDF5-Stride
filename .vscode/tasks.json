{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build slab example",
            "type": "shell",
            "command": "./build.sh",
            "options": {
                "cwd": "${workspaceFolder}/4_slab-with-stride"
            }
        },

    ]
}