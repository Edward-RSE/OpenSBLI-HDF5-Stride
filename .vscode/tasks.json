{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build wave",
            "type": "shell",
            "command": "./build.sh",
            "options": {
                "cwd": "${workspaceFolder}/src-wave"
            }
        },
        {
            "label": "Clean up build",
            "type": "shell",
            "command": "rm -rf build/",
            "options": {
                "cwd": "${workspaceFolder}/src-wave"
            }
        },
        {
            "label": "Build taylorgreen",
            "type": "shell",
            "command": "./build.sh",
            "options": {
                "cwd": "${workspaceFolder}/src-taylor-green"
            }
        }
    ]
}